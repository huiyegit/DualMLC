# Install torch first, matched to your CUDA version:
#   https://pytorch.org/get-started/locally/
torch>=2.1
transformers>=4.40        # works with both the 4.x and 5.x APIs
peft>=0.10
scipy>=1.10
numpy>=1.24
safetensors>=0.4
